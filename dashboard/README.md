# Ava//Cell Dashboard

Next.js 14 Institutional Dashboard.

## Setup

1. **Install**:
   ```bash
   npm install
   ```

2. **Run**:
   ```bash
   npm run dev
   ```

3. **Env**:
   Updates `lib/config.ts` or allow `.env.local` to override RPC URL.
