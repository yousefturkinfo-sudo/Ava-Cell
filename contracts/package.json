{
    "name": "contracts",
    "version": "1.0.0",
    "description": "Smart contracts for Ava//Cell",
    "scripts": {
        "test": "npx hardhat test",
        "compile": "npx hardhat compile",
        "deploy": "npx hardhat run scripts/deployAll.ts --network avaCell"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0",
        "dotenv": "^16.3.1",
        "@openzeppelin/contracts": "^5.0.0"
    }
}